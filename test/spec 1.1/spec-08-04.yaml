%YAML 1.1
---
- name: spec-08-04
  yaml: |
    - !<!> foo
    - !<$:?> bar
  tree: |
    +STR
      +DOC
        +SEQ
          =VAL <!> :foo
          =VAL <$:?> :bar
        -SEQ
      -DOC
    -STR
